
struct Outer
{
	byte fz (fieldsize 7) satisfies{1} then{print(fz);};  // this will contain the value 6 after reading file
	struct A
	{
		struct C
		{
			byte bb (fieldsize fz); // fieldsize = 6
		}[fz]xx;
	
	}[*] a (fieldsize 79); // fieldsize is in bits !
}bb;

file "x:/test/file_test.dat" inp_file;
read(inp_file,bb);
print(bb);

